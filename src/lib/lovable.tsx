
import React from 'react';

// Mock provider for development - replace with actual Lovable integration when available
export function withLovable(children: React.ReactNode) {
  return <>{children}</>;
}
